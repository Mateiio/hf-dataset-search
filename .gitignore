__pycache__/
*.py[cod]
.venv/
venv/
.env

# Raw EML is refetched by harvest.py, not committed (31 MB).
data/eml/

# Intermediate build artefacts
*.pkl
.DS_Store
.idea/
.vscode/

# EDI corpus (edisearch/). Listings and raw EML are refetched by the probe
# and the harvester, not committed. records.jsonl ships once M1 produces it.
data/probe/
data/raw/
data/evalset/fulltext/
data/evalset/datasets/
# working copies; the packed .gz / .f16.npy versions ship instead (GitHub's 100 MB limit)
data/records.jsonl
data/edi_embeddings.npy
